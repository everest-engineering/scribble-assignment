# Feature Tasks: Scenario 2 — Game Start & Drawer Flow

## Tasks
- [ ] Add `drawerId` and `secretWord` to backend room model.
- [ ] Implement `POST /rooms/:code/start` with host and participant count validation.
- [ ] Add start request schema validation.
- [ ] Add `api.startGame()` on the frontend.
- [ ] Implement host-only Start button in `LobbyPage.tsx`.
- [ ] Add redirect to `/game` when room status becomes `game`.
- [ ] Update `GamePage.tsx` to show drawer and guesser views.
- [ ] Add tests for host assignment, drawer role, and secret word masking.
- [ ] Manually verify game start and drawer visibility across two tabs.
